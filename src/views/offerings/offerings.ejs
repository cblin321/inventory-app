<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../navbar'); -%>

   <h1>Offerings for: <%= offerings.length > 0 ? offerings[0].course_number : "No Offerings" %></h1> 
   <% offerings.map((offering) => { %>
    <p><%= `${offering.semester} ${offering.year}`  %></p>
    <p><%= `Start time: ${offering.offering_time_start}`%></p>
    <p><%= `End time: ${offering.offering_time_end}`  %></p>
    <p><%= `Course capacity: ${offering.capacity}`  %></p>
    <p><%= `Currently enrolled: ${offering.num_enrolled}`  %></p>
    <button onclick="window.location.href = '<%= offering.updateURL %>'">Update</button>
    <form method="POST" action="<%= offering.deleteURL %>">
        <button type="submit">Delete Course Offering</button>
    </form>
   <% }) %>
    <button onclick="window.location.href='<%= addURL %>'">Add Course Offering</button>
</body>
</html>
