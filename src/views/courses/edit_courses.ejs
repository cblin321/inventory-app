<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1>Updating <%= `${course.course_number}: ${course_name}` %></h1>
  <form action="/courses/add/" method="POST" >
    <input type="text" name="course_number" placeholder="Course number" value="<%= course.course_number %>">

    <input type="text" name="course_name" placeholder="Course name" value="<%= course.course_name %>">

    <button type="submit">Add Course</button>
  </form>  
</body>
</html>